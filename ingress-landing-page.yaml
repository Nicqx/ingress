apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-landing-page
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
      - "pmqxyz.hopto.org"
    secretName: my-tls-secret
  rules:
  - host: "pmqxyz.hopto.org"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: landing-page-service
            port:
              number: 80
